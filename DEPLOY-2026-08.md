# 2026-08 バックエンド更新のデプロイ手順

対象: クローラ堅牢化・/health追加・画像ドメイン修正・クロールcronのコンテナ化

## 変更内容

| リポジトリ | 変更 |
|---|---|
| news-app-backend-refactor (develop) | クローラ堅牢化(1サイト失敗で全停止しない)・`GET /health`・フォールバック画像ドメイン修正 |
| news-app-docker | app: v0.0.21タグ・healthcheck・`AP_ROOT`修正 / cronコンテナ追加 / nginx: `/health`公開 |
| DB | `scripts/2026-08-01-fix-image-domain.sql`(既存8サイトのアイコン・RSS URL更新) |

## 手順(ローカル)

```bash
# 1. 新イメージをビルドしてpush(developの最新を取り込む)
cd ~/Documents/workspace/news-app-docker
docker build -t gcr.io/gke-test-287910/news-app:v0.0.21 --platform amd64 --no-cache app
docker push gcr.io/gke-test-287910/news-app:v0.0.21
```

## 手順(VM: ssh keisuke877jp@35.197.79.235)

```bash
# 2. news-app-docker の変更を反映(git pull なり rsync なり従来の方法で)

# 3. ⚠️ 旧クロールcronを無効化(cronコンテナと二重実行になるため)
crontab -l            # 現状確認(/v1/stock を叩く行を控える)
crontab -e            # 該当行を削除 or コメントアウト

# 4. コンテナ更新
cd <news-app-dockerの配置先>
docker compose pull app
docker compose up -d

# 5. DB修正(実行前にsitesテーブルを確認)
mysql -h 127.0.0.1 -P 3306 -u root -ppassword -D matome < scripts/2026-08-01-fix-image-domain.sql
```

## 確認

```bash
curl -s https://matome.folks-chat.com/health          # {"status":"ok"}
curl -s https://matome.folks-chat.com/v1/site | jq '.data[].image'   # 全て matome.folks-chat.com
docker compose logs cron --tail 5                     # 10分ごとに "stock ok"
docker compose ps                                     # app が healthy
```
