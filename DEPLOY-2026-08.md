# 2026-08 バックエンド更新のデプロイ手順

対象: クローラ堅牢化・/health追加・画像ドメイン修正

- サーバ: `ssh keisuke877jp@34.173.153.189`(鍵: `~/.ssh/id_ed25519`。MEMO.md記載の35.197.79.235は旧IP)
- composeは**v1**(`docker-compose`コマンド。`docker compose`は使えない)
- RSSクロールはVMのsystemdタイマー `stock.timer`(2分ごと)が実行中。**そのまま維持する**

## 2026-08-01 実施済み(サーバ側)

- MySQL(3306)/Redis(6379)/app(8000)のホストバインドを127.0.0.1へ変更(外部公開はnginx:80のみ)
- `PermitRootLogin no`、journald上限200M、osconfig-agent停止、旧イメージ・バックアップ掃除
- サーバのgit checkoutを整理(8d2df7bへリセット後、ポート変更のみ未コミットで適用中)
- Ubuntu Pro attach(esm-infraによるセキュリティ更新)

## 変更内容(未デプロイ分)

| リポジトリ | 変更 |
|---|---|
| news-app-backend-refactor (develop) | クローラ堅牢化(1サイト失敗で全停止しない)・`GET /health`・フォールバック画像ドメイン修正 |
| news-app-docker (main) | app: v0.0.21タグ・healthcheck・`AP_ROOT`修正・ポート127.0.0.1化 / nginx: `/health`公開 |
| DB | `scripts/2026-08-01-fix-image-domain.sql`(既存8サイトのアイコン・RSS URL更新) |

## 手順(ローカル)

```bash
# 1. 新イメージをビルドしてpush(developの最新を取り込む)
cd ~/Documents/workspace/news-app-docker
docker build -t gcr.io/gke-test-287910/news-app:v0.0.21 --platform amd64 --no-cache app
docker push gcr.io/gke-test-287910/news-app:v0.0.21
```

## 手順(VM: ssh keisuke877jp@34.173.153.189)

```bash
cd ~/news-app-docker

# 2. リポジトリを最新化(ポート変更は未コミットのローカル編集として当たっているため一度捨てる。
#    同じ変更はmainにコミット済みなのでpull後も内容は同じ)
git checkout -- docker-compose.yml
git pull origin main

# 3. コンテナ更新(compose v1)
sudo docker-compose pull app nginx
sudo docker-compose up -d

# 4. DB修正(実行前にsitesテーブルを確認)
mysql -h 127.0.0.1 -P 3306 -u root -ppassword -D matome < scripts/2026-08-01-fix-image-domain.sql
```

## 確認

```bash
curl -s https://matome.folks-chat.com/health          # {"status":"ok"}
curl -s https://matome.folks-chat.com/v1/site | grep -o "matome-kun.ga" | wc -l   # 0件
sudo docker ps                                        # app が healthy
journalctl -u stock.service --since "10 min ago" | tail   # クロールが回っている
```
